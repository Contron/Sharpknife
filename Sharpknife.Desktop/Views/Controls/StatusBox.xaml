<UserControl x:Name="Control" x:Class="Sharpknife.Desktop.Views.Controls.StatusBox" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
	<StatusBar>
		<StatusBarItem HorizontalAlignment="Left">
			<TextBlock Text="{Binding ElementName=Control, Path=Status.Message, FallbackValue=Message}"></TextBlock>
		</StatusBarItem>
		<StatusBarItem HorizontalAlignment="Right">
			<StackPanel Orientation="Horizontal">
				<ItemsControl ItemsSource="{Binding ElementName=Control, Path=Children}">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<StackPanel Orientation="Horizontal"></StackPanel>
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
				</ItemsControl>
				<ProgressBar Visibility="{Binding ElementName=Control, Path=Status.Visibility}" IsIndeterminate="True" Width="100" Height="10" Margin="10, 0, 0, 0"></ProgressBar>
			</StackPanel>
		</StatusBarItem>
	</StatusBar>
</UserControl>
