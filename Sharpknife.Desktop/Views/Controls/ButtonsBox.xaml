<UserControl x:Name="Control" x:Class="Sharpknife.Desktop.Views.Controls.ButtonsBox" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
	<Border BorderBrush="{x:Static SystemColors.ControlDarkBrush}" BorderThickness="0,1,0,0" Background="{x:Static SystemColors.ControlBrush}">
		<StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
			<Button Content="OK" Command="{Binding OkCommand, ElementName=Control}" Visibility="{Binding OkVisibility, ElementName=Control}" IsEnabled="{Binding OkEnabled, ElementName=Control}" IsDefault="True" Width="75"></Button>
			<Button Content="Cancel" Command="{Binding CancelCommand, ElementName=Control}" Visibility="{Binding CancelVisibility, ElementName=Control}" IsEnabled="{Binding CancelEnabled, ElementName=Control}" IsCancel="True" Width="75" Margin="10,0,0,0"></Button>
		</StackPanel>
	</Border>
</UserControl>
